id: rfid_gate
label: Gate
category: RFID
templates:
  imports: import rfid
  make: rfid.gate(${sample_rate})
parameters:
- id: sample_rate
  label: Sample Rate
  dtype: int
inputs:
- label: in
  domain: stream
  dtype: complex
outputs:
- label: out
  domain: stream
  dtype: complex
file_format: 1
